import styles from "./style.module.css";
import cn from "classnames";

const Navigation = () => {
  return (
    <nav className={styles.navigation}>
      <p>html from navigation</p>
    </nav>
  );
};

export default Navigation;
